[project]
name = "kaoyan-guide-backend"
version = "0.1.0"
description = "FastAPI backend for RAG-first kaoyan guide system"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
