[project]
name = "learning-growth-backend"
version = "0.1.0"
description = "FastAPI backend for a RAG and task planning learning growth system"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
